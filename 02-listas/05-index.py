lista = [8, 5, 4, 3, 9, 1, 2, 6, 7]

print(8 in lista)  # True
# Conocer el índice de un elemento (va a retornar el PRIMER elemento encontrado)
print(lista.index(8))  # 0
# Si no encuentra el elemento arroja error, así que primero confirmar que existe
